<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Scheduler App</title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
    <script defer type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
  </head>
  <body>
      <!-- Credit to Nat Tuck's photoblog/multi lectures
https://github.com/NatTuck/scratch-2021-01/blob/master/notes-4550/11-photoblog/notes.md-->        
    <div role="main" class="container">
        
        <nav class = "navbar navbar-expand-sm navbar-light bg-light">

                            <a class="navbar-brand" href="#"><h1> Events Scheduler </h1></a>

            <a class="nav-link"><%= link "Home", to: Routes.page_path(@conn, :index)%></a>
                   
        
                        <a class="nav-link"><%= link "Users", to: Routes.user_path(@conn, :index)%></a>
         
                        <a class="nav-link"><%= link "Events", to: Routes.event_path(@conn, :index)%></a>
       

 
                    
                    <%= if @conn.assigns[:current_user] do %>
                        <li class="nav-link"> 
                            User: <%= @current_user.name %> |
                                <%= link "Logout", to: Routes.session_path(@conn, :delete), method: :delete%>
                        </li>
                    <% else %>
                  
                            <a class="nav-link"><%= link "Login", to: Routes.login_path(@conn, :index) %></a>
                        

                    <% end %>

        </nav>
      <%= if get_flash(@conn, :info) do %>
        <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <% end %>
      <%= if get_flash(@conn, :error) do %>
        <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
      <% end %>

    <%= render @view_module, @view_template, assigns %>
    </div>

  </body>
</html>




